name: Release Firefox for Android Build

on:
    workflow_dispatch:
        inputs:
            releaseTag:
                description: 'Release tag'
                required: true
                type: string
jobs:
    submit:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
              with:
                  ref: ${{ inputs.releaseTag }}

            - name: Setup Node.js and pnpm
              uses: ./.github/actions/setup-node-pnpm

            - run: pnpm install --filter @project/extension --filter @project/common
            - run: pnpm --filter @project/extension run zip:firefox-android
            - name: Submit to stores
              working-directory: ./extension
              run: |
                  pnpm wxt submit \
                    --firefox-zip .output/*-firefox-android.zip --firefox-sources-zip .output/*-sources.zip \
                    --firefox-channel listed
              env:
                  FIREFOX_EXTENSION_ID: 'asbplayer-android'
                  FIREFOX_JWT_ISSUER: ${{ secrets.FIREFOX_JWT_ISSUER }}
                  FIREFOX_JWT_SECRET: ${{ secrets.FIREFOX_JWT_SECRET }}
